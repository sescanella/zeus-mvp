# Repo Cleanup — 2026-04-22

## What happened

A comprehensive cleanup of the repository was performed on branch `chore/repo-cleanup`. Backup tag: `pre-cleanup-2026-04-22` (pushed to origin).

## Scope

This commit (`chore: archive .planning/ valuables to docs/archive/`) only covers the `.planning/` removal. Subsequent commits in the same PR handle:

- Dead backend code (`backend/services/spool_service.py` v1, broken integration tests, `backend/tests/`, `backend/prompts/`).
- One-off scripts and obsolete migration docs.
- Broken CI workflows.
- CLAUDE.md compaction with WCAG and MCP content extracted to `docs/`.

## What was rescued to `docs/archive/`

51 files out of 126 (~40%) kept as forensic reference:

| Category | Count | Rationale |
|---|---|---|
| Milestone audits (v3.0, v4.0, v5.0) | 3 | Post-mortem of what each milestone shipped |
| Codebase snapshots | 7 | Architecture/structure/conventions references |
| Top-level docs | 2 | `RETROSPECTIVE.md`, `PROJECT.md` |
| Debug post-mortems | 39 | Root-cause write-ups (forensic value when similar symptoms reappear) |

## What was deleted

75 files removed with no substitute:

- `v*-REQUIREMENTS.md`, `v*-ROADMAP.md` — redundant with audits.
- `milestones/v5.0-phases/` — 8 phases × ~8 GSD artifacts each (PLAN/SUMMARY/RESEARCH/CONTEXT/VALIDATION/VERIFICATION).
- `quick/`, `research/`, `roadmap/phase-07/`, `fixes/T-021-PLAN.md`.
- `STATE.md`, `MILESTONES.md`, `config.json`, `v5.0-single-page/`.
- `.planning/.planning/` — duplicated directory (accidental).

## Why GSD was abandoned

The project no longer uses the `/gsd:*` workflow. Commands and state were removed from the repo. The operator tracks open work in an external orchestrator (`ASISTENTE`, documented in `CLAUDE.md`). Commits reference task IDs directly.

## Reversal

If this cleanup turns out to have deleted something important:

```bash
git checkout pre-cleanup-2026-04-22 -- <path>
```

The tag is pushed to origin and will not be deleted.
