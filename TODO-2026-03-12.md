# TODO - 12 de Marzo 2026

## 1. Multi-select en selector de spools

**Objetivo:** Permitir seleccionar varios spools antes de ejecutar una accion (INICIAR/FINALIZAR).

**Estado actual:**
- El componente `SpoolTable.tsx` YA soporta multi-select con checkboxes (`selectedSpools: string[]`)
- Pero NO esta integrado en el flujo — `page.tsx` usa `selectedSpool: SpoolCardData | null` (singular)
- El modal stack actual es: Card click -> OperationModal -> ActionModal -> WorkerModal (todo single-spool)

**Cambios necesarios:**

### Frontend
- [ ] Modificar `page.tsx`: cambiar `selectedSpool` de `SpoolCardData | null` a `SpoolCardData[]`
- [ ] Crear UI de seleccion multiple en SpoolCardList o integrar SpoolTable existente
- [ ] Agregar indicador visual de "N spools seleccionados" antes de proceder
- [ ] Modificar WorkerModal para iterar sobre array de spools y ejecutar API por cada uno
- [ ] Manejar errores parciales (ej: 3 de 5 spools fallan)

### Backend
- [ ] Evaluar si se necesita endpoint batch (ej: `POST /api/v4/occupation/batch-iniciar`) o si se llama N veces al endpoint individual
- [ ] Si batch: crear `BatchIniciarRequest` con `tag_spools: list[str]` + respuesta parcial

### Consideraciones
- Google Sheets tiene limite de 60 writes/min — con 10 spools x 3 columnas = 30 writes
- Evaluar si `batch_update_by_column_name` puede escribir multiples filas en una sola llamada API
- UX: Mostrar progreso "Procesando spool 3/5..." o hacer todo en background

---

## 2. Boton X para descartar spools de la lista

**Objetivo:** Agregar un boton X a cada SpoolCard para poder eliminar spools que fueron mal agregados.

**Estado actual:**
- `SpoolCard.tsx` NO tiene boton de eliminar
- `SpoolListContext.tsx` YA tiene `removeSpool(tag)` implementado
- Los tags se persisten en localStorage (`zeues_v5_spool_tags`)

**Cambios necesarios:**

### Frontend
- [ ] Agregar boton X (icono) en la esquina superior derecha del SpoolCard
- [ ] Llamar `removeSpool(tag)` del SpoolListContext al hacer click
- [ ] Agregar confirmacion rapida o permitir undo (opcional, evaluar UX)
- [ ] Asegurar que el boton X no interfiera con el click en la card (stopPropagation)
- [ ] Accesibilidad: `aria-label="Eliminar spool {tag}"`, keyboard support

### Archivos a modificar
- `zeues-frontend/components/SpoolCard.tsx` — agregar boton X
- `zeues-frontend/components/SpoolCardList.tsx` — pasar callback onRemove

---

## 3. Registrar Armador al INICIAR armado

**Objetivo:** Cuando se inicia armado (INICIAR + ARM), escribir en la columna `Armador` de Operaciones las iniciales e ID del trabajador.

**Estado actual:**
- `Armador` se escribe SOLO durante COMPLETAR (FINALIZAR con todas las uniones) en `occupation_service.py` linea 1420
- Durante INICIAR solo se escriben: `Ocupado_Por`, `Fecha_Ocupacion`, `Estado_Detalle`
- Formato esperado: `"MR(93)"` (iniciales + ID entre parentesis)
- La columna `Armador` es parte de las v2.1 columns (indice fallback 34)

**Cambios necesarios:**

### Backend
- [ ] En `occupation_service.py` metodo `iniciar_spool()` (linea ~695-759):
  - Agregar `Armador` al batch_updates cuando `operacion == 'ARM'`
  - Valor: `worker_nombre` (ya disponible en el contexto, formato "MR(93)")
- [ ] Verificar que el column map incluye "Armador" (ya validado en schema startup)

### Ejemplo de cambio
```python
# En iniciar_spool(), seccion de batch_updates (~linea 700)
batch_updates = [
    {"row": row_num, "column_name": "Ocupado_Por", "value": worker_nombre},
    {"row": row_num, "column_name": "Fecha_Ocupacion", "value": timestamp},
    {"row": row_num, "column_name": "Estado_Detalle", "value": estado_detalle},
]
# NUEVO: Registrar Armador al iniciar ARM
if request.operacion == "ARM":
    batch_updates.append({"row": row_num, "column_name": "Armador", "value": worker_nombre})
```

### Tests
- [ ] Agregar test unitario: INICIAR ARM escribe Armador
- [ ] Agregar test unitario: INICIAR SOLD NO escribe Armador

---

## 4. Registrar Fecha_Armado al FINALIZAR armado

**Objetivo:** Al finalizar armado (COMPLETAR ARM), registrar la fecha en la columna `Fecha_Armado`.

**Estado actual:**
- **YA IMPLEMENTADO** en `occupation_service.py` linea ~1420:
  ```python
  if operacion == "ARM":
      updates["Fecha_Armado"] = format_date_for_sheets(today_chile())
      updates["Armador"] = worker_nombre
  ```
- Se escribe SOLO cuando la accion es COMPLETAR (todas las uniones finalizadas)
- NO se escribe durante PAUSAR (parcial)
- Formato: `"21-01-2026"` (DD-MM-YYYY)

**Conclusion:** Esta tarea YA esta implementada. Verificar que funciona correctamente en produccion.

### Verificacion
- [ ] Test manual: INICIAR ARM -> FINALIZAR ARM (COMPLETAR) -> verificar columna Fecha_Armado en Sheets
- [ ] Confirmar que el formato es DD-MM-YYYY
- [ ] Confirmar que PAUSAR NO escribe Fecha_Armado

---

## 5. Misma mecanica para Soldadores

**Objetivo:** Replicar los puntos 3 y 4 para soldadura: registrar `Soldador` al INICIAR y `Fecha_Soldadura` al FINALIZAR.

### 5a. Registrar Soldador al INICIAR soldadura

**Estado actual:**
- `Soldador` se escribe SOLO durante COMPLETAR (linea ~1427)
- NO se escribe durante INICIAR

**Cambios necesarios:**

### Backend
- [ ] En `occupation_service.py` metodo `iniciar_spool()`:
  - Agregar `Soldador` al batch_updates cuando `operacion == 'SOLD'`
  - Valor: `worker_nombre`

### Ejemplo de cambio
```python
# Complemento al cambio del TODO #3
if request.operacion == "ARM":
    batch_updates.append({"row": row_num, "column_name": "Armador", "value": worker_nombre})
elif request.operacion == "SOLD":
    batch_updates.append({"row": row_num, "column_name": "Soldador", "value": worker_nombre})
```

### 5b. Registrar Fecha_Soldadura al FINALIZAR soldadura

**Estado actual:**
- **YA IMPLEMENTADO** en `occupation_service.py` linea ~1427:
  ```python
  elif operacion == "SOLD":
      updates["Fecha_Soldadura"] = format_date_for_sheets(today_chile())
      updates["Soldador"] = worker_nombre
  ```

**Conclusion:** Fecha_Soldadura YA esta implementada. Solo falta registrar Soldador al INICIAR.

### Tests
- [ ] Agregar test: INICIAR SOLD escribe Soldador
- [ ] Agregar test: INICIAR ARM NO escribe Soldador
- [ ] Verificar que FINALIZAR SOLD (COMPLETAR) sigue escribiendo Fecha_Soldadura

---

## Resumen de prioridades

| # | Tarea | Complejidad | Ya implementado? |
|---|-------|-------------|-----------------|
| 1 | Multi-select spools | Alta | Parcial (SpoolTable existe) |
| 2 | Boton X en SpoolCard | Baja | No (removeSpool ya existe en context) |
| 3 | Armador al INICIAR | Baja | No (solo en COMPLETAR) |
| 4 | Fecha_Armado al FINALIZAR | - | SI, ya implementado |
| 5a | Soldador al INICIAR | Baja | No (solo en COMPLETAR) |
| 5b | Fecha_Soldadura al FINALIZAR | - | SI, ya implementado |

### Orden sugerido de ejecucion
1. **TODO #3 + #5a** (Armador/Soldador al INICIAR) — cambio backend minimo, alto valor
2. **TODO #2** (Boton X) — cambio frontend simple, mejora UX inmediata
3. **TODO #4 + #5b** — solo verificacion, ya estan implementados
4. **TODO #1** (Multi-select) — cambio mas complejo, planificar bien antes de ejecutar
