# TODO - 12 de Marzo 2026

## 1. Multi-add en AddSpoolModal

**Objetivo:** Permitir agregar varios spools a la lista de seguimiento sin cerrar el modal cada vez.

**Estado:** COMPLETADO

**Implementado:**
- [x] Revertido multi-select de procesamiento batch (fue implementado por error)
- [x] AddSpoolModal queda abierto tras agregar un spool, input se limpia
- [x] Contador verde "N spools agregados" dentro del modal
- [x] Boton "LISTO" para cerrar cuando termine de agregar
- [x] Spools ya agregados se muestran deshabilitados (greyed out)

**Aclaracion:** El procesamiento (INICIAR/FINALIZAR/PAUSAR) sigue siendo single-spool. Card click -> modal chain -> procesa UN spool.

---

## 2. Boton X para descartar spools de la lista

**Objetivo:** Agregar un boton X a cada SpoolCard para eliminar spools mal agregados.

**Estado:** COMPLETADO

**Implementado:**
- [x] Boton X en esquina superior derecha de SpoolCard (44x44px touch target)
- [x] `stopPropagation` para no triggear click de card
- [x] `removeSpool(tag)` del SpoolListContext
- [x] Accesibilidad: `aria-label`, keyboard support (Enter/Space)
- [x] Props: `onRemove` en SpoolCard y SpoolCardList, conectado en page.tsx

---

## 3. Registrar Armador al INICIAR armado

**Objetivo:** Al iniciar armado (INICIAR + ARM), escribir `Armador` en Operaciones con las iniciales e ID del trabajador.

**Estado:** COMPLETADO

**Implementado:**
- [x] `occupation_service.py` iniciar_spool(): escribe `Armador` cuando operacion == "ARM"
- [x] Formato: `"MR(93)"` (worker_nombre)
- [x] Test: INICIAR ARM escribe Armador
- [x] Test: INICIAR ARM NO escribe Soldador

---

## 4. Registrar Fecha_Armado al FINALIZAR armado

**Estado:** YA ESTABA IMPLEMENTADO (pre-existente)

- [x] `occupation_service.py` COMPLETAR ARM escribe `Fecha_Armado` con `format_date_for_sheets(today_chile())`
- [ ] Verificacion manual pendiente en produccion

---

## 5. Misma mecanica para Soldadores

### 5a. Registrar Soldador al INICIAR soldadura

**Estado:** COMPLETADO

- [x] `occupation_service.py` iniciar_spool(): escribe `Soldador` cuando operacion == "SOLD"
- [x] Test: INICIAR SOLD escribe Soldador
- [x] Test: INICIAR SOLD NO escribe Armador

### 5b. Registrar Fecha_Soldadura al FINALIZAR soldadura

**Estado:** YA ESTABA IMPLEMENTADO (pre-existente)

- [x] `occupation_service.py` COMPLETAR SOLD escribe `Fecha_Soldadura`
- [ ] Verificacion manual pendiente en produccion

---

## 6. FINALIZAR no debe preguntar quien — usar el trabajador que INICIO

**Objetivo:** Cuando se selecciona FINALIZAR (o PAUSAR/CANCELAR), no mostrar WorkerModal. El trabajador se obtiene automaticamente del campo `Ocupado_Por` del spool (quien hizo el INICIAR).

**Estado:** COMPLETADO

**Contexto:**
- Al hacer INICIAR, se escribe `Ocupado_Por` con el formato `"MR(93)"` (iniciales + ID)
- El spool card ya muestra el worker ocupante
- `page.tsx` ya tiene `parseWorkerIdFromOcupadoPor()` que extrae el ID numerico
- FINALIZAR/PAUSAR/CANCELAR siempre aplican al worker que tiene el spool ocupado

**Cambios necesarios:**

### Frontend — Implementado
- [x] `page.tsx` handleSelectAction(): FINALIZAR/PAUSAR llaman API directamente sin WorkerModal
- [x] worker_id obtenido de `selectedSpool.ocupado_por` via `parseWorkerIdFromOcupadoPor()`
- [x] Helper `executeDirectAction()` encapsula llamadas API para ARM/SOLD/REP
- [x] WorkerModal solo se muestra para INICIAR
- [x] Overlay "PROCESANDO..." con spinner durante API call
- [x] TSC + ESLint + Build pasan

### Flujo actual vs propuesto
```
ACTUAL:    Card -> Operacion -> Accion(FINALIZAR) -> WorkerModal -> API
PROPUESTO: Card -> Operacion -> Accion(FINALIZAR) -> API (worker auto del spool)
```

### Consideraciones
- Validar que `ocupado_por` no sea null/vacio antes de auto-finalizar
- Si por alguna razon `ocupado_por` esta vacio, mostrar error (no deberia pasar si el flujo es correcto)
- CANCELAR de spool libre (sin ocupado_por) sigue siendo frontend-only removal

---

## Resumen

| # | Tarea | Estado |
|---|-------|--------|
| 1 | Multi-add en AddSpoolModal | COMPLETADO |
| 2 | Boton X en SpoolCard | COMPLETADO |
| 3 | Armador al INICIAR | COMPLETADO |
| 4 | Fecha_Armado al FINALIZAR | Pre-existente (verificar) |
| 5a | Soldador al INICIAR | COMPLETADO |
| 5b | Fecha_Soldadura al FINALIZAR | Pre-existente (verificar) |
| 6 | FINALIZAR sin preguntar worker | COMPLETADO |
