---
phase: 02-core-location-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zeues-frontend/components/NotificationToast.tsx
  - zeues-frontend/components/SpoolCard.tsx
  - zeues-frontend/components/SpoolCardList.tsx
  - zeues-frontend/__tests__/components/NotificationToast.test.tsx
  - zeues-frontend/__tests__/components/SpoolCard.test.tsx
  - zeues-frontend/__tests__/components/SpoolCardList.test.tsx
autonomous: true
requirements: [CARD-02, CARD-06, STATE-05, STATE-06, MODAL-07, UX-02, UX-04]

must_haves:
  truths:
    - "NotificationToast renders success/error toasts with role=alert for screen readers"
    - "SpoolCard displays TAG, operacion, estado, worker, and elapsed timer when occupied"
    - "SpoolCard shows static PAUSADO badge without timer when estado_trabajo is PAUSADO"
    - "SpoolCardList renders multiple cards and shows empty state when no spools"
    - "Timer counts up from fecha_ocupacion in real time (1-second tick)"
  artifacts:
    - path: "zeues-frontend/components/NotificationToast.tsx"
      provides: "Toast rendering with ARIA live region"
      exports: ["NotificationToast"]
    - path: "zeues-frontend/components/SpoolCard.tsx"
      provides: "Individual spool card with timer and state badges"
      exports: ["SpoolCard", "SpoolCardProps"]
    - path: "zeues-frontend/components/SpoolCardList.tsx"
      provides: "Container for SpoolCards with empty state"
      exports: ["SpoolCardList"]
    - path: "zeues-frontend/__tests__/components/NotificationToast.test.tsx"
      provides: "Unit tests for NotificationToast"
    - path: "zeues-frontend/__tests__/components/SpoolCard.test.tsx"
      provides: "Unit tests for SpoolCard including timer and state badges"
    - path: "zeues-frontend/__tests__/components/SpoolCardList.test.tsx"
      provides: "Unit tests for SpoolCardList including empty state"
  key_links:
    - from: "zeues-frontend/components/NotificationToast.tsx"
      to: "zeues-frontend/hooks/useNotificationToast.ts"
      via: "Toast type import"
      pattern: "import.*Toast.*useNotificationToast"
    - from: "zeues-frontend/components/SpoolCard.tsx"
      to: "zeues-frontend/lib/types.ts"
      via: "SpoolCardData type import"
      pattern: "import.*SpoolCardData.*types"
    - from: "zeues-frontend/components/SpoolCardList.tsx"
      to: "zeues-frontend/components/SpoolCard.tsx"
      via: "SpoolCard component import"
      pattern: "import.*SpoolCard"
---

<objective>
Create the three new visual components for the v5.0 single-page architecture: NotificationToast (API feedback), SpoolCard (individual spool display with live timer), and SpoolCardList (container with empty state).

Purpose: These are the core building blocks for Phase 4 page assembly. SpoolCard is the primary UX surface showing spool state, operation, worker, and elapsed time. NotificationToast provides API feedback. SpoolCardList arranges cards and handles the zero-spools case.

Output: 3 new components with full test coverage, WCAG 2.1 AA compliant, Blueprint Industrial palette.
</objective>

<execution_context>
@/Users/sescanella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sescanella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v5.0-single-page/ROADMAP.md
@.planning/v5.0-single-page/REQUIREMENTS.md
@.planning/phases/02-core-location-tracking/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts from Phase 1 that these components consume -->

From zeues-frontend/lib/types.ts:
```typescript
export type EstadoTrabajo =
  | 'LIBRE' | 'EN_PROGRESO' | 'PAUSADO' | 'COMPLETADO'
  | 'RECHAZADO' | 'BLOQUEADO' | 'PENDIENTE_METROLOGIA';

export type OperacionActual = 'ARM' | 'SOLD' | 'REPARACION' | null;

export interface SpoolCardData {
  tag_spool: string;
  ocupado_por: string | null;
  fecha_ocupacion: string | null;
  estado_detalle: string | null;
  total_uniones: number | null;
  uniones_arm_completadas: number | null;
  uniones_sold_completadas: number | null;
  pulgadas_arm: number | null;
  pulgadas_sold: number | null;
  operacion_actual: OperacionActual;
  estado_trabajo: EstadoTrabajo | null;
  ciclo_rep: number | null;
}
```

From zeues-frontend/hooks/useNotificationToast.ts:
```typescript
export type ToastType = 'success' | 'error';

export interface Toast {
  id: string;
  message: string;
  type: ToastType;
}
```

From zeues-frontend/lib/spool-state-machine.ts:
```typescript
export type Operation = 'ARM' | 'SOLD' | 'MET' | 'REP';
export type Action = 'INICIAR' | 'FINALIZAR' | 'PAUSAR' | 'CANCELAR';
export function getValidOperations(spool: SpoolCardData): Operation[];
export function getValidActions(spool: SpoolCardData): Action[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: NotificationToast + SpoolCard components with tests</name>
  <files>
    zeues-frontend/components/NotificationToast.tsx,
    zeues-frontend/components/SpoolCard.tsx,
    zeues-frontend/__tests__/components/NotificationToast.test.tsx,
    zeues-frontend/__tests__/components/SpoolCard.test.tsx
  </files>
  <behavior>
    NotificationToast:
    - Renders nothing visible when toasts array is empty (container div stays in DOM for ARIA)
    - Renders success toast with green border and role="alert"
    - Renders error toast with red border and role="alert"
    - Renders multiple toasts simultaneously
    - Dismiss button calls onDismiss with toast id
    - Container has aria-live="polite" (always in DOM before first toast)
    - axe accessibility audit passes (jest-axe)

    SpoolCard:
    - Renders tag_spool prominently
    - Renders operacion_actual badge (ARM/SOLD/REP/MET or none)
    - Renders estado_trabajo badge with correct color per state (LIBRE=white, EN_PROGRESO=orange, PAUSADO=yellow, COMPLETADO=green, RECHAZADO=red, PENDIENTE_METROLOGIA=blue, BLOQUEADO=dark-red)
    - Renders ocupado_por worker name when present
    - Shows elapsed timer (MM:SS or HH:MM:SS) when ocupado_por is non-null — uses setInterval 1s tick
    - Timer parses fecha_ocupacion in "DD-MM-YYYY HH:MM:SS" format (Chilean) — NEVER use new Date() directly
    - PAUSADO state shows static "PAUSADO" badge WITHOUT timer (STATE-06)
    - Calls onCardClick(spool) when card is clicked
    - Has onRemove callback for X button
    - Keyboard accessible: Enter/Space triggers onCardClick, focus ring visible
    - axe accessibility audit passes
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    **NotificationToast.tsx:**
    - `'use client'` directive
    - Props: `{ toasts: Toast[], onDismiss: (id: string) => void }`
    - Import `Toast` type from `@/hooks/useNotificationToast`
    - Outer container: `<div aria-live="polite" aria-atomic="false" className="fixed top-4 right-4 z-[100] flex flex-col gap-2 pointer-events-none">` — ALWAYS in DOM (even when empty)
    - Each toast: `<div key={toast.id} role="alert" className="pointer-events-auto ...">` with success=green-400/error=red-400 border on zeues-navy bg
    - Dismiss button with `aria-label="Cerrar notificacion"` and `focus:ring-2 focus:ring-white focus:ring-inset`
    - Use Lucide `X` icon for dismiss (not text x character), `CheckCircle` for success, `AlertCircle` for error
    - Blueprint palette: `bg-zeues-navy border-4` with `font-mono font-black text-sm`

    **SpoolCard.tsx:**
    - `'use client'` directive
    - Props interface `SpoolCardProps`:
      ```
      spool: SpoolCardData
      onCardClick: (spool: SpoolCardData) => void
      onRemove: (tag: string) => void
      ```
    - Import `SpoolCardData` from `@/lib/types`
    - Import `getValidOperations` from `@/lib/spool-state-machine` (display available ops as info)
    - **Timer logic** — inline `useElapsedSeconds` hook:
      - Only runs when `spool.ocupado_por !== null && spool.ocupado_por !== ''`
      - Parse `fecha_ocupacion` with regex: `/^(\d{2})-(\d{2})-(\d{4})\s+(\d{2}):(\d{2}):(\d{2})$/`
      - `setInterval(tick, 1000)` with `clearInterval` cleanup
      - Format as `MM:SS` (under 1h) or `HH:MM:SS` (1h+)
      - Returns `null` when no fecha_ocupacion or parse fails
    - **PAUSADO guard (STATE-06):** When `estado_trabajo === 'PAUSADO'`, show "PAUSADO" badge, do NOT render timer even if ocupado_por is non-null
    - **State color map:**
      - LIBRE: `text-white border-white/30`
      - EN_PROGRESO: `text-zeues-orange border-zeues-orange`
      - PAUSADO: `text-yellow-400 border-yellow-400`
      - COMPLETADO: `text-green-400 border-green-400`
      - RECHAZADO: `text-red-400 border-red-400`
      - PENDIENTE_METROLOGIA: `text-blue-300 border-blue-300`
      - BLOQUEADO: `text-red-600 border-red-600 opacity-75`
    - Layout: `border-4` card on `bg-zeues-navy`, tag_spool in large `text-lg font-black font-mono`, operation badge, state badge, worker line, timer line
    - Remove button (X) top-right corner with `aria-label="Quitar spool {tag_spool} del listado"`, calls `onRemove(spool.tag_spool)`, `e.stopPropagation()` to not trigger onCardClick
    - Card clickable: `role="button" tabIndex={0}` with Enter/Space keyboard handler
    - `focus:outline-none focus:ring-2 focus:ring-zeues-orange focus:ring-inset`
    - Mobile-first: `min-h-[4rem]` touch target

    **Test files:** Use `@testing-library/react` (render, screen, fireEvent, act), `jest-axe` for a11y, `jest.useFakeTimers()` for timer tests. Mock `setInterval`/`Date.now` for deterministic timer testing.
  </action>
  <verify>
    <automated>cd zeues-frontend && npx jest --testPathPatterns="NotificationToast.test|SpoolCard.test" --no-coverage</automated>
  </verify>
  <done>
    - NotificationToast renders success/error toasts with correct ARIA attributes
    - SpoolCard renders all 7 estado_trabajo states with correct colors
    - Timer shows elapsed time when occupied, hidden when PAUSADO
    - fecha_ocupacion parsed correctly from DD-MM-YYYY HH:MM:SS format
    - All tests pass, axe audit clean, tsc --noEmit clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SpoolCardList container with empty state</name>
  <files>
    zeues-frontend/components/SpoolCardList.tsx,
    zeues-frontend/__tests__/components/SpoolCardList.test.tsx
  </files>
  <behavior>
    - Renders a list of SpoolCard components from spools array
    - Shows empty state message when spools array is empty ("No hay spools en tu lista. Usa el boton Anadir Spool para comenzar.")
    - Empty state has an icon (PackageOpen from Lucide or similar)
    - Each card receives onCardClick and onRemove callbacks passed through
    - Multiple spools render multiple cards (CARD-06)
    - axe accessibility audit passes
  </behavior>
  <action>
    **Write tests first (RED), then implement (GREEN).**

    **SpoolCardList.tsx:**
    - `'use client'` directive
    - Props interface `SpoolCardListProps`:
      ```
      spools: SpoolCardData[]
      onCardClick: (spool: SpoolCardData) => void
      onRemove: (tag: string) => void
      ```
    - Import `SpoolCard` from `@/components/SpoolCard`
    - Import `SpoolCardData` from `@/lib/types`
    - Empty state: when `spools.length === 0`, render centered message with `PackageOpen` icon (from lucide-react), text "No hay spools en tu lista", subtext "Usa el boton Anadir Spool para comenzar"
    - Non-empty: render `<div className="flex flex-col gap-4">` containing `spools.map(spool => <SpoolCard key={spool.tag_spool} spool={spool} onCardClick={onCardClick} onRemove={onRemove} />)`
    - Blueprint palette: empty state text in `text-white/50 font-mono`, icon in `text-white/30`

    **Tests:** Verify empty state renders when spools=[], verify N cards render for N spools, verify onCardClick/onRemove propagate.
  </action>
  <verify>
    <automated>cd zeues-frontend && npx jest --testPathPatterns="SpoolCardList.test" --no-coverage</automated>
  </verify>
  <done>
    - SpoolCardList renders empty state when no spools
    - SpoolCardList renders correct number of SpoolCard components
    - Callbacks propagate from list to individual cards
    - All tests pass, axe audit clean
  </done>
</task>

</tasks>

<verification>
```bash
cd zeues-frontend && npx jest --testPathPatterns="NotificationToast|SpoolCard|SpoolCardList" --no-coverage && npx tsc --noEmit
```
All component tests pass, TypeScript compiles without errors.
</verification>

<success_criteria>
- NotificationToast renders toast queue with ARIA live region (MODAL-07, UX-02)
- SpoolCard renders all spool states with correct visual differentiation (CARD-02, UX-04)
- SpoolCard timer ticks when occupied, hidden when PAUSADO (STATE-05, STATE-06)
- SpoolCardList renders multiple cards with empty state (CARD-06)
- All tests green, tsc --noEmit clean, axe audits pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-location-tracking/02-01-SUMMARY.md`
</output>
