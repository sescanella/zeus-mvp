---
phase: 05-limpieza
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - zeues-frontend/tests/accessibility.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Accessibility tests validate v5.0 single-page modal architecture"
    - "No test references deleted page routes"
    - "axe-core scans run against main page and modal states"
    - "Full build + lint + test pipeline passes"
  artifacts:
    - path: "zeues-frontend/tests/accessibility.spec.ts"
      provides: "v5.0 accessibility test suite"
      min_lines: 30
  key_links:
    - from: "zeues-frontend/tests/accessibility.spec.ts"
      to: "http://localhost:3000/"
      via: "Playwright page.goto"
      pattern: "page\\.goto.*localhost:3000"
---

<objective>
Rewrite accessibility tests for the v5.0 single-page modal architecture. All 6 existing tests navigate through old multi-page routes that no longer exist after Plan 01 deletions.

Purpose: Maintain WCAG 2.1 Level AA compliance testing for the new architecture.
Output: Updated accessibility.spec.ts that tests the single-page + modal stack flow.
</objective>

<execution_context>
@/Users/sescanella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sescanella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-limpieza/05-RESEARCH.md
@.planning/phases/05-limpieza/05-01-SUMMARY.md

<interfaces>
<!-- v5.0 page structure (from Phase 4 implementation) -->
From zeues-frontend/app/page.tsx:
- Default export: Page wraps HomePage in SpoolListProvider
- HomePage renders: "Anadir Spool" button + SpoolCardList + NotificationToast
- Modal stack: AddSpoolModal, OperationModal, ActionModal, WorkerModal, MetrologiaModal

<!-- Current accessibility test (being rewritten) -->
From zeues-frontend/tests/accessibility.spec.ts:
- 6 tests all navigate through old multi-page flow (/, /operacion, /tipo-interaccion, /seleccionar-spool)
- Uses AxeBuilder from @axe-core/playwright
- Tags: wcag2a, wcag2aa, wcag21a, wcag21aa
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite accessibility.spec.ts for v5.0 single-page modal architecture</name>
  <files>zeues-frontend/tests/accessibility.spec.ts</files>
  <action>
    Rewrite the entire `tests/accessibility.spec.ts` file. Replace all 6 old tests with new tests for the v5.0 architecture.

    **New test structure (5-6 tests):**

    1. **Main page has no a11y violations** — Navigate to `/`, wait for page load, run axe scan with wcag2a/wcag2aa/wcag21a/wcag21aa tags. The page shows "Anadir Spool" button and empty SpoolCardList.

    2. **"Anadir Spool" button is keyboard accessible** — Tab to the "Anadir Spool" button (look for text matching "ANADIR SPOOL" or "Anadir Spool" or similar), verify it receives focus (check focused element), press Enter, verify AddSpoolModal opens (look for modal dialog or SpoolTable content appearing).

    3. **AddSpoolModal has no a11y violations** — After opening AddSpoolModal (click "Anadir Spool" button), run axe scan. The modal contains SpoolTable + SpoolFilterPanel.

    4. **Keyboard navigation: Collapsible filter panel in AddSpoolModal** — Open AddSpoolModal, Tab to filter toggle button (has aria-controls="filter-panel"), press Enter to expand, verify aria-expanded="true" and filter inputs become visible.

    5. **SpoolTable rows in AddSpoolModal are keyboard accessible** — After opening AddSpoolModal, verify table rows have role="button", tabIndex="0", and aria-label containing "spool". This test may need the dev server to have spool data loaded — if no data, check that the table renders with proper ARIA even in empty state.

    6. **Modal ESC key closes top modal** — Open AddSpoolModal, press Escape, verify modal closes and focus returns to the "Anadir Spool" button.

    **Important notes:**
    - All tests start at `http://localhost:3000/` (single page)
    - Keep using AxeBuilder from @axe-core/playwright with tags ['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa']
    - These are Playwright E2E tests — they require a running dev server
    - Use reasonable timeouts (5s for waits) since we depend on a real or mocked backend
    - If no backend data is available, axe scans should still pass on the empty state
    - Do NOT test flows that require actual API data (INICIAR/FINALIZAR etc.) — those depend on backend state. Focus on structural a11y.
  </action>
  <verify>
    <automated>cd zeues-frontend && npx tsc --noEmit && grep -c "test(" tests/accessibility.spec.ts</automated>
  </verify>
  <done>accessibility.spec.ts contains 5-6 tests for v5.0 architecture. No references to old routes (/operacion, /tipo-interaccion, /seleccionar-spool). TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Final full pipeline verification</name>
  <files>zeues-frontend/</files>
  <action>
    Run the complete verification pipeline to confirm the entire phase is clean:

    ```bash
    cd zeues-frontend
    npx tsc --noEmit        # TypeScript clean
    npm run build            # Production build
    npm run lint             # ESLint clean
    npm test                 # Jest unit tests
    ```

    All 4 commands must pass with 0 errors and 0 warnings.

    **Note on Playwright tests:** `npx playwright test tests/accessibility.spec.ts` requires a running dev server with the app. This is an E2E test and may not be runnable in the current CI context. Document whether it was run or skipped in the SUMMARY.

    If any failures appear, fix them. Common issues:
    - Unused imports in files that were not directly modified but imported from deleted modules
    - ESLint warnings about unused variables
    - Jest snapshot mismatches if any snapshots referenced old components
  </action>
  <verify>
    <automated>cd zeues-frontend && npm run build 2>&1 | tail -3 && npm run lint 2>&1 | tail -3 && npm test -- --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Full pipeline passes: tsc, build, lint, test. Phase 5 cleanup is complete. Zero dead code from old multi-page flow remains.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd zeues-frontend && npx tsc --noEmit` — 0 errors
2. `cd zeues-frontend && npm run build` — success
3. `cd zeues-frontend && npm run lint` — 0 warnings
4. `cd zeues-frontend && npm test` — all tests pass
5. `grep -c "test(" zeues-frontend/tests/accessibility.spec.ts` — 5+ tests
6. `grep -c "operacion\|tipo-interaccion\|seleccionar-spool" zeues-frontend/tests/accessibility.spec.ts` — 0 matches
</verification>

<success_criteria>
- accessibility.spec.ts fully rewritten for v5.0 single-page modal architecture
- No references to deleted page routes in any test file
- Full build pipeline green (tsc + build + lint + test)
- Phase 5 cleanup complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-limpieza/05-02-SUMMARY.md`
</output>
