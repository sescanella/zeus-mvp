---
phase: 01-migration-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zeues-frontend/lib/types.ts
  - zeues-frontend/lib/api.ts
  - zeues-frontend/lib/local-storage.ts
  - zeues-frontend/__tests__/lib/local-storage.test.ts
autonomous: true
requirements: [CARD-02, CARD-03, API-01, API-02]

must_haves:
  truths:
    - "SpoolCardData interface matches backend SpoolStatus model exactly (snake_case, same fields)"
    - "IniciarRequest.worker_nombre is optional (matches backend Plan 00-03 change)"
    - "FinalizarRequest accepts action_override field (matches backend Plan 00-03 change)"
    - "getSpoolStatus(tag) calls GET /api/spool/{tag}/status and returns SpoolCardData"
    - "batchGetStatus(tags) calls POST /api/spools/batch-status and returns SpoolCardData[]"
    - "loadTags returns [] on empty/malformed localStorage"
    - "addTag persists tag and returns updated list"
    - "removeTag removes tag and returns updated list"
    - "localStorage functions are no-ops when window is undefined (SSR safe)"
  artifacts:
    - path: "zeues-frontend/lib/types.ts"
      provides: "SpoolCardData, EstadoTrabajo, OperacionActual types + updated IniciarRequest/FinalizarRequest"
      contains: "SpoolCardData"
    - path: "zeues-frontend/lib/api.ts"
      provides: "getSpoolStatus, batchGetStatus functions"
      contains: "getSpoolStatus"
    - path: "zeues-frontend/lib/local-storage.ts"
      provides: "loadTags, saveTags, addTag, removeTag, clearTags"
      contains: "STORAGE_KEY"
    - path: "zeues-frontend/__tests__/lib/local-storage.test.ts"
      provides: "Unit tests for localStorage persistence"
      min_lines: 40
  key_links:
    - from: "zeues-frontend/lib/api.ts"
      to: "zeues-frontend/lib/types.ts"
      via: "import SpoolCardData"
      pattern: "import.*SpoolCardData.*from.*types"
    - from: "zeues-frontend/lib/api.ts"
      to: "/api/spool/{tag}/status"
      via: "fetch call"
      pattern: "api/spool/.*status"
---

<objective>
Create the data layer foundation for v5.0: TypeScript types (SpoolCardData interface matching backend SpoolStatus), new API client functions (getSpoolStatus, batchGetStatus), and localStorage persistence utility for spool tags.

Purpose: All Phase 2-4 components depend on these types, API functions, and persistence layer. Without them, no card can render, no batch refresh can happen, and no spool list can persist.

Output: Extended `lib/types.ts`, extended `lib/api.ts`, new `lib/local-storage.ts`, and tests for localStorage.
</objective>

<execution_context>
@/Users/sescanella/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sescanella/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-migration-foundation/01-RESEARCH.md

@zeues-frontend/lib/types.ts
@zeues-frontend/lib/api.ts
@backend/models/spool_status.py

<interfaces>
<!-- Backend SpoolStatus model — frontend SpoolCardData must mirror this exactly -->
From backend/models/spool_status.py:
```python
class SpoolStatus(BaseModel):
    tag_spool: str
    ocupado_por: Optional[str] = None
    fecha_ocupacion: Optional[str] = None
    estado_detalle: Optional[str] = None
    total_uniones: Optional[int] = None
    uniones_arm_completadas: Optional[int] = None
    uniones_sold_completadas: Optional[int] = None
    pulgadas_arm: Optional[float] = None
    pulgadas_sold: Optional[float] = None
    operacion_actual: Optional[str] = None   # "ARM" | "SOLD" | "REPARACION" | None
    estado_trabajo: Optional[str] = None     # "LIBRE" | "EN_PROGRESO" | "PAUSADO" | "COMPLETADO" | "RECHAZADO" | "BLOQUEADO" | "PENDIENTE_METROLOGIA"
    ciclo_rep: Optional[int] = None          # 1-3 for RECHAZADO/REPARACION

class BatchStatusResponse(BaseModel):
    spools: list[SpoolStatus]
    total: int
```

From zeues-frontend/lib/api.ts (existing pattern):
```typescript
async function handleResponse<T>(response: Response): Promise<T>
export class ApiError extends Error { status: number; detail?: string; }
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8000';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SpoolCardData types and update IniciarRequest/FinalizarRequest</name>
  <files>zeues-frontend/lib/types.ts</files>
  <behavior>
    - SpoolCardData has all 12 fields matching backend SpoolStatus (snake_case)
    - EstadoTrabajo is a union type of 7 string literals
    - OperacionActual is 'ARM' | 'SOLD' | 'REPARACION' | null
    - IniciarRequest.worker_nombre is optional (string | undefined)
    - FinalizarRequest has action_override?: 'PAUSAR' | 'COMPLETAR' field
    - FinalizarRequest.selected_unions is optional (not needed when action_override is set)
  </behavior>
  <action>
    Add to zeues-frontend/lib/types.ts (after existing interfaces, before v4.0 section or at the top):

    1. Add `EstadoTrabajo` union type: `'LIBRE' | 'EN_PROGRESO' | 'PAUSADO' | 'COMPLETADO' | 'RECHAZADO' | 'BLOQUEADO' | 'PENDIENTE_METROLOGIA'`
    2. Add `OperacionActual` type: `'ARM' | 'SOLD' | 'REPARACION' | null`
    3. Add `SpoolCardData` interface with all 12 fields matching backend SpoolStatus model exactly (use snake_case, NOT camelCase):
       - tag_spool: string
       - ocupado_por: string | null
       - fecha_ocupacion: string | null
       - estado_detalle: string | null
       - total_uniones: number | null
       - uniones_arm_completadas: number | null
       - uniones_sold_completadas: number | null
       - pulgadas_arm: number | null
       - pulgadas_sold: number | null
       - operacion_actual: OperacionActual
       - estado_trabajo: EstadoTrabajo | null
       - ciclo_rep: number | null
    4. Update `IniciarRequest`: change `worker_nombre: string` to `worker_nombre?: string` (Plan 00-03 made it optional in backend)
    5. Update `FinalizarRequest`: add `action_override?: 'PAUSAR' | 'COMPLETAR'` field, make `selected_unions` optional (`selected_unions?: string[]`) since action_override bypasses union selection

    IMPORTANT: Keep snake_case field names — they must match the JSON response from the backend exactly. Do NOT convert to camelCase.
  </action>
  <verify>
    <automated>cd zeues-frontend && npx tsc --noEmit</automated>
  </verify>
  <done>SpoolCardData interface exported, IniciarRequest/FinalizarRequest updated, tsc --noEmit passes</done>
</task>

<task type="auto">
  <name>Task 2: Add getSpoolStatus and batchGetStatus to API client</name>
  <files>zeues-frontend/lib/api.ts</files>
  <action>
    Add two new functions to zeues-frontend/lib/api.ts. Import SpoolCardData from './types'.

    1. `getSpoolStatus(tag: string): Promise<SpoolCardData>` — wraps GET /api/spool/{tag}/status. Uses handleResponse<SpoolCardData>(res) directly. No custom error handling beyond what handleResponse provides.

    2. `batchGetStatus(tags: string[]): Promise<SpoolCardData[]>` — wraps POST /api/spools/batch-status with body `{ tags }`. Response shape is `{ spools: SpoolCardData[], total: number }` — extract and return `data.spools`.

    Follow existing pattern in api.ts:
    - Use `${API_URL}/api/spool/${tag}/status` and `${API_URL}/api/spools/batch-status`
    - Use `handleResponse<T>` for response parsing
    - Add SpoolCardData to the import from './types'
    - Both use `headers: { 'Content-Type': 'application/json' }`
    - batchGetStatus uses method: 'POST' with JSON.stringify({ tags }) body

    Place them in a new section comment: `// ==========================================`
    `// v5.0 SPOOL STATUS API FUNCTIONS`
    `// ==========================================`

    Add them BEFORE the existing v4.0 section.
  </action>
  <verify>
    <automated>cd zeues-frontend && npx tsc --noEmit</automated>
  </verify>
  <done>getSpoolStatus and batchGetStatus exported from api.ts, tsc passes, functions follow existing handleResponse pattern</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create localStorage persistence utility with tests</name>
  <files>zeues-frontend/lib/local-storage.ts, zeues-frontend/__tests__/lib/local-storage.test.ts</files>
  <behavior>
    - loadTags() returns [] when localStorage is empty
    - loadTags() returns [] when localStorage has malformed JSON
    - loadTags() returns [] when localStorage has non-array JSON (e.g., a string, object)
    - loadTags() returns string[] when localStorage has valid JSON array of strings
    - loadTags() filters out non-string elements from array
    - saveTags(tags) writes JSON.stringify(tags) under key 'zeues_v5_spool_tags'
    - addTag(tag) adds tag to list, returns updated list
    - addTag(tag) does NOT add duplicate tag, returns existing list
    - removeTag(tag) removes tag from list, returns updated list
    - removeTag(nonexistent) returns unchanged list
    - clearTags() removes the key from localStorage
    - All functions are SSR-safe (no-op when typeof window === 'undefined')
  </behavior>
  <action>
    1. Create zeues-frontend/lib/local-storage.ts with the following functions:
       - `const STORAGE_KEY = 'zeues_v5_spool_tags'` (versioned key per D-02)
       - `loadTags(): string[]` — SSR guard, try/catch JSON.parse, Array.isArray guard, filter for string elements
       - `saveTags(tags: string[]): void` — SSR guard, JSON.stringify
       - `addTag(tag: string): string[]` — load, skip if includes, append, save, return
       - `removeTag(tag: string): string[]` — load, filter, save, return
       - `clearTags(): void` — SSR guard, removeItem

    2. Create zeues-frontend/__tests__/lib/local-storage.test.ts with tests covering all 12 behaviors listed above.
       - Use jest's built-in localStorage mock (jsdom environment provides it)
       - Clear localStorage in beforeEach
       - Test SSR safety by temporarily mocking window as undefined if possible, or by verifying the guard logic directly

    IMPORTANT: Use `typeof window !== 'undefined'` guard on every localStorage access (Next.js SSR compatibility).
    IMPORTANT: Do NOT use `any` type anywhere — use `unknown` for parsed JSON.
  </action>
  <verify>
    <automated>cd zeues-frontend && npx jest --testPathPattern="local-storage" --no-coverage</automated>
  </verify>
  <done>local-storage.ts exports 5 functions, all tests pass, tsc --noEmit passes, no `any` types used</done>
</task>

</tasks>

<verification>
```bash
cd zeues-frontend && npx tsc --noEmit && npx jest --testPathPattern="local-storage" --no-coverage
```
</verification>

<success_criteria>
- SpoolCardData interface exists with 12 fields matching backend SpoolStatus exactly
- IniciarRequest.worker_nombre is optional, FinalizarRequest has action_override
- getSpoolStatus and batchGetStatus are exported from api.ts
- localStorage utility has 5 exported functions with SSR guards
- All localStorage tests pass
- tsc --noEmit passes with zero errors
- No `any` types used anywhere
</success_criteria>

<output>
After completion, create `.planning/phases/01-migration-foundation/01-01-SUMMARY.md`
</output>
